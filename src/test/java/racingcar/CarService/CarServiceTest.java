package racingcar.CarService;

import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import racingcar.domain.Car;
import racingcar.service.CarService;

import java.util.ArrayList;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class CarServiceTest {
//
//    @Test
//    void playGameNoOneGoTest() {
//        List<Car> carsList = new ArrayList<>();
//        for (int i = 0; i < 3; i++) {
//            carsList.add(new Car("car" + i, 0));
//        }
//        int randomCount = 3;
//
//        CarService carService = new CarService();
//        carService.playGame(carsList, randomCount);
//
//        Assertions.assertThat(carsList.get(0).getGoCount()).isEqualTo(0);
//        Assertions.assertThat(carsList.get(1).getGoCount()).isEqualTo(0);
//        Assertions.assertThat(carsList.get(2).getGoCount()).isEqualTo(0);
//
////        Mockito.verify()
//    }

}