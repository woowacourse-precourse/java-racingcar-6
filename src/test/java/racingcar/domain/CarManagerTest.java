package racingcar.domain;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

import java.util.*;

import static org.junit.jupiter.api.Assertions.*;

class CarManagerTest {

    CarManager carManager = new CarManager();
    /*

    @Test
    void Map의_Key값_내림차순(){
        CarManager.carScore.put(1,"밴츠");
        CarManager.carScore.put(4,"부가티");

        List<Integer> result = new ArrayList<>(CarManager.carScore.keySet());

        assertThat(result).isEqualTo(Arrays.asList(4, 1)); //[4,1]
    }

    @Test
    void 큰값을_가진_value_출력(){
        CarManager.carScore.put(1,"밴츠");
        CarManager.carScore.put(4,"부가티");
        CarManager.carScore.put(4,"마세라티");

        List<String> result = carManager.winnerData();

        assertThat(result).isEqualTo("[부가티, 마세라티]");

    }
    */

}