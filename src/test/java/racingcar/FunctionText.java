package racingcar;

import org.junit.jupiter.api.Test;

public class FunctionText {
    @Test
    void split_메서드로_주어진_값을_구분() {
        String input = "pobi,java";
        //List<String> result = Racingcar_Game.userInput(input);

        //assertThat(result).contains("java", "pobi");
        //assertThat(result).containsExactly("pobi", "java");
    }

}
