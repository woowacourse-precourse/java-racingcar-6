내가 고민한 것들

전체 라운드를 입력받고 남은 라운드가 있는지 확인하는 역할을 Race 클래스가 관리할 것인지, Round 클래스를 만들어 관리할 것인지
-> Round 클래스를 만들어 관리한다. 이유는 레이스 자체에 대한 정보이기 때문에?

Race 클래스는 model에 포함되는지, controller에 포함되는지
-> controller에 포함된다. 자동차의 이동/정지를 판단하는 기능, 승자를 판단하는 기능 등 carList를 활용한 기능이 레이스의 규칙과 기능이라 생각하기 때문
 Race 클래스에서 carList를 반환받아 또 다른 컨트롤러 클래스가 활용하게 구현할 수 있지만